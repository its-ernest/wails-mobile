# hello-world sync configuration
# Use sync.sh to build the AAR and dump generated files into the Android template libs folder.

[build]
gomobile_target = android/arm64
androidapi = 21
package = github.com/its-ernest/wails-mobile/_examples/example
aar_name = wailsmobile.aar
output_dir = native/android/app/libs
clean_output = true
